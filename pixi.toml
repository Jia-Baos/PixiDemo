[workspace]
authors = ["jia-baos <18383827268@163.com>"]
channels = ["conda-forge", "./output"]
name = "PixiDemo"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[feature.dev.dependencies]
cmake = "*"
ninja = "*"
gdb = "*"
patchelf = "*"
rattler-build = ">=0.64.1,<0.65"

[feature.test.dependencies]
gtest = "*"
pixi_demo = ">=1.0.0,<2"

[environments]
dev = ["dev"]
test = ["test"]

# 全局依赖（所有环境共享）
[dependencies]
